{
  "name": "pdf-merge",
  "version": "1.0.0",
  "description": "Merges provided PDFs and splits them into even and odd segments",
  "main": "dist/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "spuggle",
  "license": "MIT",
  "dependencies": {
    "pdf-lib": "^1.9.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.14",
    "typescript": "^3.9.2"
  }
}
